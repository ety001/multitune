# Changelog

本文件记录多音盒 MultiTune 的版本变更。各版本详情见 [GitHub Releases](https://github.com/ety001/multitune/releases)。

## v0.0.8（2026-09-01）

文档修订版本，无代码变更（代码与 v0.0.7 一致，包含车机版图标居中修复）。

- **验证方法文档修订**（`docs/car-ui-icon-centering-verification.md` v1.1）：修正封面音符「偏右」表述（像素实测水平居中，系 FA 音符字形不对称的视觉印象）；修正 115% 字体放大的机制归因——加深旧方案偏差的是「系统文字缩放连容器字号一起放大」（strut 变化、基线下移），仅放大图标字号不会加深偏移；补充独立复核数据：新 CSS 在容器字号 ×1.15 条件下偏差 ≤1 物理px，对文字缩放鲁棒

## v0.0.7（2026-09-01）

车机版播放控制按钮图标与封面占位音符居中修复。

- **修复车机版控制按钮图标不在圆形按钮中央**：图标原先依赖 `line-height` + `vertical-align: middle` 垂直居中，FontAwesome 字体自带 `line-height: 1`，其字体度量与父级字体不一致，导致图标整体下沉（48px 按钮内偏下约 3px，大按钮更多）。改为绝对定位 + `translate(-50%,-50%)` 几何居中，只依赖按钮尺寸、与字体度量无关；同时清理 UA 默认 button `padding` 与各断点已失效的 `line-height` 声明
- **修复车机版封面占位音符不在中央**：同类问题，且 ≤480px 窄屏下封面容器缩至 120px 而 `line-height` 仍为 150px，音符偏下达约 24px；同样改为绝对定位居中（加载真实封面后 JS 用 `img` 整体替换占位符，不受影响）

## v0.0.6（2026-08-30）

方向盘/耳机媒体按键控制播放，歌单列表最近播放优先 + 大列表窗口化与搜索。

- **媒体会话接入**（#30）：车机版与完整版播放页接入媒体会话，方向盘上一曲/下一曲等媒体硬件按键可用。优先懒猫 WebShell 注入的 `lzc_media_session` 桥（老车机 WebView 无标准 MediaSession API 也能建立原生媒体会话），其次浏览器原生 `navigator.mediaSession`，均不可用时静默降级。支持 play / pause / 上一曲 / 下一曲 / 快进快退（±10 秒）/ seekto / stop；换歌同步标题、歌手、封面到系统媒体界面
- **歌单列表最近播放优先**（#30）：按最后播放时间逆序（`playlist_states.updated_at`），上次听的歌单永远排第一；从未播放的按原顺序兜底
- **歌单列表阈值窗口化**（#30）：新增 `PLAYLIST_WINDOW_THRESHOLD` 环境变量（默认 36，接口随列表下发 `window_threshold`），歌单数超过阈值时完整版（网格行对齐切片 + ResizeObserver 列数自适应）与车机版（复用歌曲列表虚拟滚动模式）切换窗口化渲染，DOM 数量恒定；阈值内保持全量渲染
- **歌单搜索**（#30）：车机版歌单弹层与完整版歌单页新增搜索框；阈值内前端即输即滤，超过阈值 300ms 防抖走后端 `?q=`（LIKE 通配符转义为字面量匹配）
- **修复车机版封面图未铺满容器**（#30）：封面 `img` 为内联元素，坐在容器 `line-height` 文本基线上整体下移，顶部露出背景色；改为 `display: block` 后完全铺满，容器补 `overflow: hidden` 防圆角溢出
- **修复**：移除车机版身份/歌单请求携带的无效 `?limit=100` 参数
## v0.0.5（2026-08-26）

懒猫微服存储源按用户隔离拆分、扫描任务回看、播放页长歌单性能优化。

- **媒体存储源拆分**（#26）：移除笼统的「媒体」存储源，拆为「远程挂载」（`/lzcapp/media/RemoteFS/<用户名>`，仅当前用户自己的目录）与「USB 挂载」（`/lzcapp/media/media`，共享）；RemoteFS 总根与媒体总根不再可浏览/扫描
- **移除文件浏览器多余的「扫描」按钮**（#27）：该入口只入库不加歌单、无进度反馈，与主流程重复
- **新增「扫描任务」页面**（#28）：与身份选择/文件浏览器平级的 tab，可回看扫描任务的状态、进度、新增数与失败消息；任务持久化记录扫描路径（数据库迁移 006）；扫描在服务端后台执行，关闭页面不影响结果
- **播放页歌单列表窗口化**（#29）：虚拟滚动（只渲染可视区行，DOM 数量恒定）；修复超过 200 首的歌单显示不全；歌曲详情按需加载（`song_ids` + `songs/batch`，首屏零额外请求）；窄屏播放器置顶、列表为固定高度窗口；mini-player 在播放页隐藏

## v0.0.4（2026-08-26）

弱网（车机场景）续播起播优化与封面缩略图缓存。

- **续播先跳转后播放**（#25）：修正双请求问题——原实现立即 `play()` 会先从文件头缓冲、跳转时再放弃并重新发 Range 请求，弱网下双请求抢占带宽导致起播缓慢；车机版与完整版均改为先跳转再播放，元数据 8 秒未到达兜底从头播放
- **封面缩略图缓存**：新增 `GET /api/songs/:id/cover?size=thumb`，ffmpeg 生成约 300px WebP（比原图小 90%+），懒生成 + 磁盘缓存；懒猫部署挂载 `/lzcapp/cache/thumbnails`（`CACHE_PATH`）
- **车机版封面延迟加载**：起播后再加载缩略图；失败回退原图、再回退默认图标

## v0.0.3（2026-08-26）

懒猫微服部署下的文件浏览器按当前用户隔离访问范围。

- **文件浏览器用户隔离**（#24）：懒猫部署时存储源限定为媒体目录 + `/lzcapp/document/<当前用户名>`；用户名取自网关注入的可信 header `X-HC-User-ID`，白名单校验防路径注入；严格路径段前缀匹配防绕过；扫描接口同样校验；无有效用户名返回 401
- **修复 `CreateScanJob` 数据竞争**：先序列化响应再启动后台扫描任务
- 非懒猫部署行为不变

## v0.0.2（2026-07-23）

车机版播放器集中优化与窄屏（iPhone SE 等小屏设备）修复。

- **车机版多项优化**（#21）：FontAwesome 5 图标、播放列表虚拟滚动（万级歌曲流畅）、专辑封面显示、去除触控选中框、新落地页
- 落地页调试代码清理（#22）
- **窄屏（iPhone SE）修复**（#23）：播放控制行五按钮居中对称、音量弹层竖向滑杆修复、弹层对齐、窄屏顶栏收纳为菜单弹层

## v0.0.1（2026-07-17）

多音盒首个发布版本：面向车机场景的多身份本地音乐播放器。

- **多身份多歌单**：身份/歌单管理，上车默认身份
- **双前端**：车机版（ES5，兼容 Chrome/WebView ≤ 74 老车机）+ 完整版（Vue 3）
- **记忆播放**：身份记忆点 + 歌单记忆点（曲目 + 播放位置），切换歌单自动续播
- **文件浏览器**：扫描音频文件入库，全选/批量添加到歌单
- **进度上报**：10s 周期 + 事件即时保存，超时重试与防误操作
- 后端 Go + SQLite（CGO-free），单容器部署
