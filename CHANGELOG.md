# Changelog

本文件记录多音盒 MultiTune 的版本变更。各版本详情见 [GitHub Releases](https://github.com/ety001/multitune/releases)。

## v0.0.5（2026-08-26）

懒猫微服存储源按用户隔离拆分、扫描任务回看、播放页长歌单性能优化。

- **媒体存储源拆分**（#26）：移除笼统的「媒体」存储源，拆为「远程挂载」（`/lzcapp/media/RemoteFS/<用户名>`，仅当前用户自己的目录）与「USB 挂载」（`/lzcapp/media/media`，共享）；RemoteFS 总根与媒体总根不再可浏览/扫描
- **移除文件浏览器多余的「扫描」按钮**（#27）：该入口只入库不加歌单、无进度反馈，与主流程重复
- **新增「扫描任务」页面**（#28）：与身份选择/文件浏览器平级的 tab，可回看扫描任务的状态、进度、新增数与失败消息；任务持久化记录扫描路径（数据库迁移 006）；扫描在服务端后台执行，关闭页面不影响结果
- **播放页歌单列表窗口化**（#29）：虚拟滚动（只渲染可视区行，DOM 数量恒定）；修复超过 200 首的歌单显示不全；歌曲详情按需加载（`song_ids` + `songs/batch`，首屏零额外请求）；窄屏播放器置顶、列表为固定高度窗口；mini-player 在播放页隐藏

## v0.0.4（2026-08-26）

弱网（车机场景）续播起播优化与封面缩略图缓存。

- **续播先跳转后播放**（#25）：修正双请求问题——原实现立即 `play()` 会先从文件头缓冲、跳转时再放弃并重新发 Range 请求，弱网下双请求抢占带宽导致起播缓慢；车机版与完整版均改为先跳转再播放，元数据 8 秒未到达兜底从头播放
- **封面缩略图缓存**：新增 `GET /api/songs/:id/cover?size=thumb`，ffmpeg 生成约 300px WebP（比原图小 90%+），懒生成 + 磁盘缓存；懒猫部署挂载 `/lzcapp/cache/thumbnails`（`CACHE_PATH`）
- **车机版封面延迟加载**：起播后再加载缩略图；失败回退原图、再回退默认图标

## v0.0.3（2026-08-26）

懒猫微服部署下的文件浏览器按当前用户隔离访问范围。

- **文件浏览器用户隔离**（#24）：懒猫部署时存储源限定为媒体目录 + `/lzcapp/document/<当前用户名>`；用户名取自网关注入的可信 header `X-HC-User-ID`，白名单校验防路径注入；严格路径段前缀匹配防绕过；扫描接口同样校验；无有效用户名返回 401
- **修复 `CreateScanJob` 数据竞争**：先序列化响应再启动后台扫描任务
- 非懒猫部署行为不变

## v0.0.2（2026-07-23）

车机版播放器集中优化与窄屏（iPhone SE 等小屏设备）修复。

- **车机版多项优化**（#21）：FontAwesome 5 图标、播放列表虚拟滚动（万级歌曲流畅）、专辑封面显示、去除触控选中框、新落地页
- 落地页调试代码清理（#22）
- **窄屏（iPhone SE）修复**（#23）：播放控制行五按钮居中对称、音量弹层竖向滑杆修复、弹层对齐、窄屏顶栏收纳为菜单弹层

## v0.0.1（2026-07-17）

多音盒首个发布版本：面向车机场景的多身份本地音乐播放器。

- **多身份多歌单**：身份/歌单管理，上车默认身份
- **双前端**：车机版（ES5，兼容 Chrome/WebView ≤ 74 老车机）+ 完整版（Vue 3）
- **记忆播放**：身份记忆点 + 歌单记忆点（曲目 + 播放位置），切换歌单自动续播
- **文件浏览器**：扫描音频文件入库，全选/批量添加到歌单
- **进度上报**：10s 周期 + 事件即时保存，超时重试与防误操作
- 后端 Go + SQLite（CGO-free），单容器部署
